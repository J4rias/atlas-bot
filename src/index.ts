// This file is deprecated. Use the specific entry points:
//   pnpm dev:consultant  — Mode 1: Sales bot (Express + Messenger)
//   pnpm dev:manager     — Mode 2: Business manager (Telegram + scheduler)
console.error(
  'Use "pnpm dev:consultant" or "pnpm dev:manager" instead of the root entry point.',
);
process.exit(1);
